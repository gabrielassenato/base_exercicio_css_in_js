import { Form, FormContainer, HeroTitle } from './HeroStyled'

const Hero = () => (
  <Form>
    <FormContainer>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </FormContainer>
  </Form>
)

export default Hero
